# Runtime dependencies. Pin versions for reproducible, auditable deploys.
pandas
pyarrow>=17  # star-schema Parquet exports for the BI feed (warehouse.export_star_parquet)>=3.0.3      # upgraded to 3.x (2026-07): new default string dtype + Copy-on-Write; load_extract handles both dtypes
python-dotenv>=1.2.2

# --- Web scraping / CSV downloads ---
requests>=2.34.2
beautifulsoup4>=4.15.0
lxml>=6.1.1          # fast, robust HTML parser backend for BeautifulSoup
playwright>=1.61.0   # headless browser: drives the PeopleSoft JS download flow

# --- MCP server (read-only warehouse interface for MCP clients) ---
mcp>=1.28.1,<2       # exposes warehouse.db to Claude Code; no Anthropic API calls.
                     # <2: mcp 2.0.0 (2026-07-28) removed mcp.server.fastmcp — it
                     # boot-crashed the MCP Space on a factory-reboot image rebuild
                     # and broke fresh CI installs. Migrate deliberately, not by pip.
