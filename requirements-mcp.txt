# Runtime deps for the remote MCP server image ONLY — kept lean.
# The server (src/mcp_server.py) needs `mcp` (uvicorn/starlette come transitively)
# plus matplotlib for the generate_chart / generate_report tools. None of the
# pipeline's heavy deps (pandas, playwright, …) are required.
mcp>=1.28.1
matplotlib>=3.11.1  # chart rendering (Agg backend) for generate_chart/generate_report
huggingface_hub>=1.24.0  # CommitScheduler for the optional tool-call audit log (query_log.py)
sentry-sdk>=2.66.1  # optional error tracking (src/observability.py); no-op unless SENTRY_DSN set
